import styled from "styled-components";

export const TaskCardTaskText = styled.span`
  display: inline-block;
  max-width: 490px;
  margin-bottom: 7px;
  color: ${(props) => props.theme.colors.tableBlackColor};
`;
